# 🛡️ DevSecOps AI Assistant

A Streamlit-powered chatbot that integrates with your DevSecOps GitHub Actions pipeline.

## Features

| Tab | What it does |
|-----|-------------|
| 🤖 **AI Chat** | Chat with Llama 3.3 70B about security, vulnerabilities, DevSecOps — or anything else |
| 📊 **Pipeline Dashboard** | View recent GitHub Actions runs, load artifacts with one click |
| 🔍 **Scan Results** | Browse findings from Semgrep, Trivy, tfsec, Gitleaks, Conftest — click "Explain & Fix" for AI analysis |
| 💡 **Remediation** | View per-tool remediation suggestions generated by your pipeline |

## Setup

### 1. Get a free Groq API key
1. Go to [console.groq.com](https://console.groq.com)
2. Sign up (free)
3. Create an API key

### 2. GitHub Personal Access Token
You already have this. Make sure it has `repo` and `actions:read` permissions.

### 3. Run locally
```bash
cd devsecops-chatbot
pip install -r requirements.txt
streamlit run app.py

```

### 4. Deploy to Streamlit Cloud
1. Push this folder to your GitHub repo (e.g., in a `chatbot/` directory)
2. Go to [share.streamlit.io](https://share.streamlit.io)
3. Connect your GitHub repo
4. Set the main file path to `chatbot/app.py`
5. Add secrets in Streamlit Cloud settings:
   ```toml
   ```

## Architecture

```
┌─────────────────────────────────────────────┐
│              Streamlit App                    │
│  ┌─────────┐  ┌──────────┐  ┌────────────┐ │
│  │ AI Chat │  │Dashboard │  │ Scan/Remed │ │
│  └────┬────┘  └────┬─────┘  └─────┬──────┘ │
│       │             │              │         │
│       ▼             ▼              ▼         │
│  ┌─────────┐  ┌──────────┐  ┌────────────┐ │
│  │Groq API │  │GitHub API│  │  Artifacts  │ │
│  │Llama3.3 │  │ Actions  │  │  Parser     │ │
│  └─────────┘  └──────────┘  └────────────┘ │
└─────────────────────────────────────────────┘
```

## File Structure

```
devsecops-chatbot/
├── app.py                    ← Main Streamlit app
├── requirements.txt          ← Python dependencies
├── .streamlit/
│   └── config.toml           ← Dark theme config
└── README.md                 ← This file
```


How to get started
Step 1: Sign up at console.groq.com and get a free API key
Step 2: Put the devsecops-chatbot/ folder in your repo and run:
bashpip install -r requirements.txt
streamlit run app.py
Step 3: Enter your Groq API key + GitHub PAT + repo name in the sidebar
Step 4 (optional): Deploy free on share.streamlit.io — connect your repo, point to app.py, add API keys as secrets

added check